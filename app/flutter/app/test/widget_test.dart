// Placeholder test file so `flutter analyze` passes.
// Sprint 1 does not include widget tests per the implementation plan.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
